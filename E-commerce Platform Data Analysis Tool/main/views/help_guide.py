"""
Page 6: 使用帮助 - 增强版
用户指南 | FAQ | 快速入门
"""

import streamlit as st
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).parent.parent))


def render():
    """显示帮助页面 - 增强版"""
    st.markdown('<h1 class="text-display">📖 使用帮助中心</h1>', unsafe_allow_html=True)
    
    st.markdown("""
    ## 电商平台数据分析工具 v3.0 Pro - 用户指南

    ### 🎯 工具简介

    本工具是一款**专业的电商数据智能分析平台**，采用现代化UI设计和AI驱动技术，为您提供：

    - 🤖 **智能采集引擎**：基于Selenium的自动化爬虫，支持天猫/淘宝平台实时采集
    - 🔐 **实时登录模式**：每次采集自动打开浏览器，安全可靠，无需手动管理Cookie
    - 📊 **多维度深度分析**：价格分布、品牌市场、消费者画像等4大分析维度
    - 📈 **专业可视化**：使用Plotly生成交互式图表，对标Tableau/Power BI
    - 💾 **一站式报告中心**：统一管理日志、数据文件、分析报告，支持批量下载
    - ⚙️ **灵活配置系统**：可自定义采集参数、输出格式、高级选项

    ---

    ### 🚀 快速开始指南（3步上手）

    #### 第1步：配置采集参数
    
    进入左侧菜单的「**数据采集**」页面：
    
    1. **设置搜索关键词**：默认"食品"，可根据需求修改（如"零食"、"饮料"）
    2. **调整采集数量**：建议首次使用选择10-50个商品进行测试
    3. **配置评价数量**：每个商品采集的评价条数（0-500条）
    4. **选择运行模式**：
       - ✅ **正常模式**：显示浏览器窗口，可观察采集过程
       - 🔇 **无头模式**：后台运行，不显示浏览器（适合服务器环境）

    #### 第2步：启动采集任务

    点击「**🚀 开始采集**」按钮后，系统将自动执行以下操作：

    ```
    点击按钮 → 自动打开Edge浏览器 → 跳转淘宝登录页 → 等待您登录 → 确认后开始采集
    ```

    **重要提示**：
    - ✅ 系统会**自动弹出浏览器窗口**，请勿关闭
    - ✅ 使用**扫码登录**或**账号密码**完成淘宝/天猫登录
    - ✅ 登录成功后，返回本工具界面点击「**✅ 我已完成登录**」按钮
    - ✅ 系统将自动开始数据采集，可在界面实时查看进度

    #### 第3步：查看分析结果

    采集完成后，您可以：

    1. **📊 数据概览**：查看采集统计（商品数、平均价格、店铺数等）
    2. **🔍 数据分析**：进入多维度分析工作台
       - 价格分布分析（直方图/箱线图）
       - 品牌市场占有率分析
       - 消费者画像与评价情感分析
       - 综合数据报告生成
    3. **📄 报告中心**：下载和管理所有生成的文件
       - JSON原始数据文件
       - 日志记录文件
       - 分析报告文档
       - CSV/Excel导出数据

    ---

    ### 📋 功能模块详解

    | 模块图标 | 功能名称 | 核心功能描述 |
    |:-------:|---------|-------------|
    | 📊 | **数据概览** | 仪表盘展示采集数据的整体情况，包括关键指标卡片、数据预览表、快速导航入口。空状态时提供一键跳转到采集页面的引导。 |
    | 📥 | **数据采集** | 智能爬虫控制台，基于状态机架构设计。支持实时登录模式、参数化配置（关键词/数量/评价）、正式采集与测试模式切换、实时进度显示。 |
    | 🔍 | **数据分析** | B端专业级数据分析工作台，提供4大分析维度：①价格分布可视化 ②品牌市场分析 ③消费者画像 ④综合报告。使用Plotly交互式图表。 |
    | 📊 | **报告中心** | 文件管理中心，支持按类型筛选（日志/JSON/报告/表格）、搜索、预览、单个或批量下载、ZIP打包导出功能。 |
    | 🍪 | **Cookie管理** | 当前采用**实时登录模式说明页**，详细解释工作原理：无需预先保存Cookie，每次采集时自动打开浏览器等待用户登录，更安全便捷。 |
    | ⚙️ | **系统设置** | 全局配置中心，包括：默认参数设置（关键词/数量/格式）、爬虫高级选项（延迟时间/重试次数）、数据清理功能、配置导入导出。 |

    ---

    ### ⚙️ 高级功能说明

    #### 🧪 测试模式 vs 正式采集

    | 特性 | 🧪 测试模式 | 🚀 正式采集 |
    |-----|-----------|-----------|
    | 适用场景 | 验证配置是否正确、初次体验 | 生产环境大规模数据采集 |
    | 采集数量 | 较少（快速验证） | 用户自定义（10-200个） |
    | 推荐使用 | 首次使用、调试问题 | 日常使用 |

    #### 🔄 实时登录模式优势

    相比传统的Cookie管理模式，当前版本采用的**实时登录模式**具有以下优势：

    - ✅ **安全性更高**：不在本地存储敏感的Cookie信息
    - ✅ **稳定性更好**：避免Cookie过期导致采集失败
    - ✅ **操作简单**：无需学习Cookie管理知识
    - ✅ **自动适配**：支持扫码、密码等多种登录方式

    #### 📈 数据分析能力

    系统内置**4大专业分析维度**：

    1. **📈 价格分布分析**
       - 商品价格区间直方图
       - 价格箱线图识别异常值
       - 平均价/中位数/标准差统计

    2. **🏪 品牌市场分析**
       - 品牌市场占有率饼图
       - TOP品牌销量排名柱状图
       - 店铺类型分布统计

    3. **👥 消费者画像**
       - 评分分布情况
       - 评价关键词云图
       - 用户购买偏好洞察

    4. **📋 综合报告**
       - 一键生成完整分析报告
       - 支持导出为Markdown/HTML格式

    ---

    ### ⚠️ 常见问题 FAQ

    **Q1: 为什么点击"开始采集"后需要登录？**
    
    A: 天猫/淘宝平台有反爬虫机制，需要模拟真实用户的登录状态才能正常访问商品数据。系统会自动打开浏览器引导您完成登录，整个过程只需30秒。

    **Q2: 采集过程中可以关闭浏览器吗？**
    
    A: ❌ **不可以**。采集期间浏览器必须保持打开状态，否则会导致采集中断。如需暂停，请点击界面上的「⏹️ 重置」按钮。

    **Q3: 采集速度慢是正常的吗？**
    
    A: ✅ **完全正常**。为了保护您的账号安全和避免被平台封禁，程序会模拟人类的浏览节奏（随机延迟3-8秒），这是必要的反检测措施。
    
    **提速建议**：
    - 减少单次采集的商品数量（建议≤50个）
    - 减少每商品的评价采集数量（建议≤50条）
    - 避免高频次连续采集（间隔≥30分钟）

    **Q4: 出现错误或采集失败怎么办？**
    
    A: 请按以下步骤排查：
    
    1. **检查网络连接**：确保网络稳定，可访问淘宝网
    2. **查看错误提示**：界面会显示具体的错误信息
    3. **检查日志文件**：在「报告中心」下载最新的.log文件查看详情
    4. **重置并重试**：点击「⏹️ 重置」按钮清理状态后重新开始
    5. **联系技术支持**：如问题持续，请提供错误日志寻求帮助

    **Q5: 支持哪些电商平台？**
    
    A: 当前版本专注于**天猫/淘宝平台**的数据采集，后续版本计划扩展至京东、拼多多等主流电商平台。

    **Q6: 采集的数据保存在哪里？**
    
    A: 所有数据默认保存在项目目录下的 `output/` 文件夹中，包括：
    - `*.json` - 结构化的商品和评价数据
    - `*.log` - 采集过程的详细日志
    - 可在「报告中心」页面统一管理和下载

    **Q7: 如何导出数据到Excel？**
    
    A: 在「报告中心」页面选中JSON数据文件后，点击「📥 批量下载」或单独下载，然后使用Excel的"数据→从JSON导入"功能打开。后续版本将直接支持一键导出Excel格式。

    ---

    ### 🔧 系统要求与技术规格

    #### 运行环境要求

    | 项目 | 最低要求 | 推荐配置 |
    |-----|---------|---------|
    | **Python版本** | ≥ 3.8 | 3.9 / 3.10 / 3.11 |
    | **操作系统** | Windows 10+ | Windows 11 |
    | **内存** | 4GB RAM | 8GB RAM及以上 |
    | **硬盘空间** | 500MB可用空间 | 2GB以上（用于存储数据） |
    | **浏览器** | Microsoft Edge (Chromium) | 最新版Edge + ChromeDriver |

    #### 安装依赖

    在项目根目录执行：
    ```bash
    pip install -r requirements.txt
    ```

    主要依赖包包括：
    - `streamlit` - Web应用框架
    - `selenium` - 浏览器自动化
    - `pandas` - 数据处理
    - `plotly` - 交互式可视化
    - `webdriver-manager` - 自动管理浏览器驱动

    #### 启动方式

    ```bash
    # 方式1: GUI模式（推荐）
    streamlit run main.py

    # 方式2: CLI命令行模式
    python main.py --mode full          # 完整流程（采集+分析+报告）
    python main.py --mode crawl         # 仅执行数据采集
    python main.py --mode analyze       # 仅执行数据分析
    python main.py --mode test          # 测试模式（使用模拟数据）
    ```

    ---

    ### 📞 技术支持与反馈

    如遇问题或功能建议，欢迎通过以下渠道反馈：

    - 📧 提交Issue：在项目仓库创建GitHub Issue
    - 📝 查看日志：在「报告中心」下载最新的运行日志
    - 🔍 版本信息：v3.0 Pro (Modern UI Edition) - 2026年发布

    """)
    
    st.markdown("---")
    
    st.subheader("快捷操作")

    quick_col1, quick_col2, quick_col3 = st.columns(3)

    with quick_col1:
        # 核心操作 - Primary按钮
        if st.button("前往采集", type="primary", use_container_width=True):
            st.session_state.pending_navigation = "数据采集"
            st.rerun()

    with quick_col2:
        # 次要操作 - Secondary按钮
        if st.button("查看分析", type="secondary", use_container_width=True):
            st.session_state.pending_navigation = "数据分析"
            st.rerun()

    with quick_col3:
        # 次要操作 - Secondary按钮
        if st.button("管理Cookie", type="secondary", use_container_width=True):
            st.session_state.pending_navigation = "Cookie管理"
            st.rerun()


from pathlib import Path
